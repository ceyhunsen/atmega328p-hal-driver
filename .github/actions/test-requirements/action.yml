name: Test requirements
description: "Local testing requirements"

runs:
  using: "composite"

  steps:
    - uses: actions/checkout@v4
      with:
        submodules: "true"
    - name: Install AVR toolchain
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-avr binutils-avr avr-libc make
